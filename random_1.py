import random
random.seed(20)
c=[1,2,3,4,6]
for i in range(0,10):
    print(random.choice(c))


#random numbers===>it will help to reduce time for manipulation
#1]ramdom.random->it will generate random numbers[only float values]
#2]random.seed->it will store the particular set of result of random numbers
#3]random.choice->it will show the own choice of the list
#4]random.randrange->here we can set the range and generate randomly.
